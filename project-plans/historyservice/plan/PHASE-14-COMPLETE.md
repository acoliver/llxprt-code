# Phase 14: Tool Call Management - Complete

The ToolCallManager class has been created in `packages/core/src/services/history/ToolCallManager.ts` with the following methods:

- `addToolCall()` 
- `updateToolCallStatus()`
- `getToolCalls()`
- `clearToolCalls()`

All methods currently throw "Not implemented yet" as required.